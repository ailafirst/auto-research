# 文档索引

**想知道系统现在长什么样** → 当前状态。**想知道某个设计为什么这么定** → 专题。**想知道某一版改了什么** → 版本说明。

## 当前状态

| 文档 | 用途 |
|---|---|
| [技术架构文档.md](技术架构文档.md) | 系统架构、8 节点流水线、模块职责、数据流、API |
| [容器化部署.md](容器化部署.md) | 当前部署形态（7 容器 + 宿主机模型服务）**为什么这么设计**。没用过 Docker 也能读；照着跑的步骤在[根 README](../README.md) |

## 调研与规划

对外看别人怎么做，对内定下一步改什么。与「专题」的区别：专题记**已经做过**的，这里记**还没做**的。

| 文档 | 主题 |
|---|---|
| [GraphRAG评测结论.md](GraphRAG评测结论.md) | **实验已终止（2026-09-08）**，本文是选型、两轮 spike、四层评测（L0-L3 全部未达标）、工程复核与补齐（阶段 A-D，判定门槛结果"停止"）、Global Search 补测（4 语料 rubric 覆盖率 graph=0.828 全面落后 dense=0.936）全过程的唯一存档，合并替代原三篇过程文档。**结论：两轮独立测评（Local Search + Global Search）均为负向，不进生产线，POC 代码/容器/conda 环境均已清理**，仅保留本文记录，重新评估的前提条件见 §7 |
| [多Agent协调架构方案.md](多Agent协调架构方案.md) | 选定 LangGraph supervisor / orchestrator-worker 模式（对齐 Anthropic 生产验证过的范式），把 8 节点直线图改造为 supervisor + 并行 worker subgraph；含 Citation Registry 并发安全改造、复杂度门控放量策略、四阶段迁移路径 |
| [深度研究端到端评测体系调研与方案.md](深度研究端到端评测体系调研与方案.md) | 从 RAG 回归扩展到端到端深度研究评测：任务 rubric、引用支持、过程轨迹、交互与运营成本；含外部基准调研与分阶段落地方案 |
| [AnySearch技术原理与项目借鉴调研.md](AnySearch技术原理与项目借鉴调研.md) | AnySearch 的检索路由、垂直领域目录、批量检索、跨源融合与内容提取原理；说明公开边界、与本项目的对应关系、接入方式和 POC 评测准则 |

## 专题：改进历程与实测

各组件的演进、**否决过的方案**与实测数据。按流水线顺序排列。

| 文档 | 主题 |
|---|---|
| [检索与证据.md](检索与证据.md) | 搜什么、从哪搜、收哪些源：Retriever V4、Tavily 接入与三层限速防护、学术双路信源实验、一次"答非所问"的全链路根因诊断 |
| [检索路由蓝图.md](检索路由蓝图.md) | 从 AnySearch 调研到域驱动路由 P0 落地全过程：垂直源覆盖（差距 #1）从 6 域扩到 10 域（含 environment 替换掉规模不达标的 ip）、中英文白名单规模化到 10/10、Planner 意图识别实测（35 题 97.1% 后被质疑样本不足，92 题重测修正为 90.2%，暴露 3 个系统性误判模式）、对照 AnySearch 找到根因（domain 曾是全局单标签）并把路由粒度下沉到子问题级、边界子问题支持双域混合检索不强制二选一（差距 #2）、接入 CrossRef + arXiv 两个结构化学术数据源（互补覆盖已发表文献与前沿预印本）+ 可解释跨源融合排序（差距 #3/#4）+ 跨 provider 去重改用 DOI/arXiv ID 等规范标识符（业界通用实体消歧做法，AnySearch 未公开对应细节）、全量 12 题基准测出两源在真实并发下被限速 + 给两源各设进程级并发上限（重跑验证发现效果分化：CrossRef 明显改善，arXiv 未见效；进一步诊断确认 arXiv 限的是短窗口累计请求数而非并发数）+ 补测 CrossRef 真实阈值后给两源都接上跨进程 Redis 令牌桶限速（复用 LLM 调用同款机制；CrossRef 效果确认——完整基准失败清零、总耗时收窄 25%；arXiv 用干净起点压力测试复核后修正了"已解决"的乐观结论——限流器机制本身工作正常，但 arXiv 仍会无差别拒绝一段时间，问题触达了客户端节流覆盖不到的层面，如实记录未达成"零 429"）+ 分速率压力测试找平衡点，确认现有配置本身合理、并排除"并发=2"是致因，同时测出"一旦触发冷却纯拼节奏救不回来"且受限状态不完全由自身请求行为决定 + 给 arXiv 加熔断（命中 429 后 360 秒内跳过不再送死），12 题基准复核 429 次数从 67 降到 6、总耗时再收窄 12.4%，同时如实记录 CrossRef 侧首次出现的零星失败，每一步都附真实数据，包括中途被推翻的错误结论和真实测出并修复的 bug |
| [RAG改进方案.md](RAG改进方案.md) | 切片 / 向量化 / 重排序 / HyDE 改写、评估体系、GraphRAG-V 否决实验 |
| [向量检索与Rerank技术说明.md](向量检索与Rerank技术说明.md) | 面试问答体：向量检索原理、embedding 模型（bge-m3）选型、Rerank 原理与常见模式对比、为什么选 Cross-Encoder、问题分层诊断思路、过期资料处理现状 |
| [模型服务化与显存内存优化.md](模型服务化与显存内存优化.md) | 多 worker 崩溃根因（commit 内存而非显存）、FP16、模型服务化 |

## 版本说明

按发布时间倒序，每份记录该版本的主线、改动统计、方法论与实测数据。全部在 [`version/`](version/)。

| 版本 | 主线 |
|---|---|
| [v0.3.5](version/v0.3.5.md) | 检索路由域驱动下沉到子问题 + CrossRef/arXiv 结构化源 + 混合检索（BM25）查询自适应上线 + GraphRAG 评测终止归档 |
| [v0.3.0](version/v0.3.0.md) | 容器化部署 + MySQL/Qdrant 数据层落地 + 凭据与 `/health` 信息暴露收敛 + 文档重组 |
| [v0.2.1](version/v0.2.1.md) | 多轮增量补证 + 全局目标锚定 + 信源与报告抗幻觉（CoVe）+ 全节点关思维链降本 |
| [v0.2.0](version/v0.2.0.md) | 多 worker 服务化 + 模型服务化/FP16 + React 前端过程透明 |
| [v0.1.6](version/v0.1.6.md) | Query Rewriting（HyDE，检索 +1.7pp）|
| [v0.1.5](version/v0.1.5.md) | RAG 检索质量：源头清洗 + retrieve_k=40 + 跨语言双路（+9.2pp）|
| [v0.1.4](version/v0.1.4.md) | RAG 切片修复 + LLM 并发体系 + Tavily 稳健性 |
