# 开发与测试工具链 —— 不进容器镜像
#
#     pip install -r requirements-dev.txt

-r requirements.txt

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# === Code Quality ===
ruff>=0.3.0
mypy>=1.8.0

# === Legacy UI ===
# ui/streamlit_app.py 是 v0.2.0 前的旧界面，当前前端是 web/（React + Vite）。
streamlit>=1.40.0
